name: testing-phasegen
channels:
  - conda-forge
dependencies:
  - python=3.10
  - msprime
  - numba
  - pip
  - pip:
    - pytest
    - pytest-xdist
    - pytest-cov
    - pytest-sugar
    - pytest-pycharm
    - -r requirements_testing.txt